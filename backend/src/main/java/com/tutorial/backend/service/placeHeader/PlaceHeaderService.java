package com.tutorial.backend.service.placeHeader;

import com.tutorial.backend.entity.PlaceHeader;

public interface PlaceHeaderService {
    PlaceHeader saveOnePlaceHeader(PlaceHeader placeHeader);
}
